class Solution {
public:
    string addBinary(string a, string b) {
        string answer = "";

        int i = a.size() - 1;

        for (int i = a.size()-1; i >=0; i--){
            if (a[i])
        }
        while (i)

        int a_num = a.size()-1;
        int b_num = b.size()-1;
        num = 0;

        while (a_num >= 0 && b_num >=0){
            num = a[a_num] + b[b_num];

        }

        if (a.size() > b.size()){
            smaller_index = b.size() - 1;
            larger_index = a.size() - 1;
        }
        else{
            smaller_index = b.size() - 1;
            larger_index = a.size() - 1;
        }

        while (smaller_index >=0){
            if()
        }

        if (a.size() > b.size()){
            last_index = b.size() - 1;
        }
        else{
            last_index = a.size() - 1;
        }
        return answer;
    }
};
